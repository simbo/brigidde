@require 'global-vars'

$terminal = {
  content: {
    padding: {
      x: .5rem,
      y: .5rem
    }
  },
  lineHeight: $lineHeight.large,
  textColors: {
    input: $colors.terminalWhite,
    inputPrompt: $colors.terminalMagenta,
    inputCommand: $colors.terminalCyan,
    message: darken($colors.terminalWhite, 20%),
    messagePrompt: darken($colors.terminalMagenta, 50%),
    command: darken($colors.terminalCyan, 20%),
    secondary: $colors.textSecondary
  }
  message: {
    spacing: .05rem,
  },
  prompt: {
    spacing: .4em,
    content: '❯'
  },
  statusBar: {
    height: 24px
  }
}
